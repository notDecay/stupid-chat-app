export const enum __ENV__ {
  api = "/duck"
}

export const enum AppRoutes {
  home = '/',
  login = '/login',
  register = '/register',
  channel = '/channel',
  acknowledgement = '/ack'
}

export const enum SocketRoutes {
  message = '/message'
}